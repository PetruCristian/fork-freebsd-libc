# fork-freebsd-libc
Fork / Extraction of FreeBSD libc file

# Script usage

1. Clone the FreeBSD source code repository
1. Run the script as such: `python3 extract_libc.py <freebsd_src_root> <output_dir>`
